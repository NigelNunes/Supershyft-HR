/** Default camp for HR dashboard API sections. Override via VITE_CAMP_NO. */
export const DEFAULT_CAMP_NO = Number(import.meta.env.VITE_CAMP_NO) || 1_280_426;

/** All Employees is only available for this camp year. */
export const EMPLOYEES_CAMP_YEAR = '2026';
